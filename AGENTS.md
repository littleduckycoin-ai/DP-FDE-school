# 飞书 FDE 学校维护入口

- 唯一 Skill 源码位于 `skills/school-guide/`，通过 `npx skills add` 分发。不要生成 `.codex/`、`.claude/`、`.agents/` 副本或 ZIP 安装包。
- 学习与会前整理读取 `skills/school-guide/SKILL.md` 和随包脚本，直接从飞书读取当前目录、规则和案例；仓库与历史归档不是教学后端。
- 学习记录只写飞书案例 reflections 正文。首次取得本人自述身份与会话授权，后续沿用；不写 Issue、评论或消息，不上传维护对话或虚构测试反馈。
- 保留授权、真实身份核验、稳定 ID 查重、完整分页和写后回读。不得改他人历史记录或扩大共享权限；不把约定冒充平台权限保护。
- 脚本只用 Node.js 标准库，飞书访问复用官方 `lark-cli`。安装与首次 OAuth 分开，不能把安装成功当作连接或保存成功。
- 修改后运行 `node --test tests/*.mjs`。安装冒烟测试放在临时目录，不改维护者的全局 Skills 或飞书配置。
- 原始教材与历史记录已归档，恢复入口见 README。不得重写或删除归档历史。维护对话不生成学员反馈。
